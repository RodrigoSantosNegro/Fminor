<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AptmC"
        tools:targetApi="31">
        <activity
            android:name=".InicioSesionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScaleCalculator"
            android:exported="true"/>
        <activity
            android:name=".ScaleName"
            android:exported="true"/>
        <activity
            android:name=".RegistroActivity"
            />
        <activity
            android:name=".MainTest"
            android:exported="true"/>
        <activity
            android:name=".SettingsApp" />


        <activity android:name=".ScaleCalculator"
            android:exported="false" />

        <activity android:name=".NoteIdentification"
            android:exported="false" />

        <activity android:name=".ChordIdentification"
            android:exported="false" />

        <!-- Corrected placement of the intent-filter -->
        <activity android:name=".IntervalIdentification"
        android:exported="false" />

        <activity android:name=".ScaleName"
            android:exported="false" />

    </application>

</manifest>

